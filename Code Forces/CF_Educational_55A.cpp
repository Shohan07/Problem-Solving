#include<bits/stdc++.h>
using namespace std;
int main()
{
    int n, fg, sg, eg, s = 0, cnt = 0;
    string s;
    cin >> n >> s;

   for(int i = 0; i < n; i++){
    if(s[i] == 'G' && s == 0){
        fg++;
    }
    else if(s[i] == '
            ')
   }
}
