#include<bits/stdc++.h>
using namespace std;
#define ll long long
map<ll, ll>mp;
int main()
{
    int t, n, i, j, k, ans;
    cin >> t;
    while(t--){
        mp.clear();
        cin >> n;
        string s;
        cin >> s;
        for(i=n; i>=0; i--){
            if(s[i]=='*'){

            }
        }
    }
}
