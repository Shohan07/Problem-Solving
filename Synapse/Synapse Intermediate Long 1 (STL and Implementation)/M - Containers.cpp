#include<bits/stdc++.h>
using namespace std;

int main()
{
    int i, l, t=1, cnt, j = 0, n;
    sring s;
    while(cin >> s){
       vector<stack<char>v;
       stack<char>st;

       c.push_back(st);
       v[0].push(s[i]);

       l = s.size();
       for(i = 0; i < l; i++)
       {
           bool f = 0;
           n = -1;
           for(j = 0; j < v.size(); j++){
            if(s[i] <= v[j].top())
           }
       }
       v.clear();
    }
}
