#include<bits/stdc++.h>
using namespace std;

long long int df(long long int n)
int main()
{
    long long int n , chc1, chc2, chc3, ans;

    cin >> n;

    if(n < 4) cout << n << endl;

    else {
        chc1 = ;
        ans = max(n, df(n/2) + df(n/3) + df(n/4);
        cout << ans << endl;
    }
    return 0;
}
