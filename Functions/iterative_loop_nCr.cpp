#include<bits/stdc++.h>
using namespace std;
int arr[10007][10007];

int nCr(int n, int r)
{

}
int main()
{
    int n, r;
    while(cin >> n >> r){
        memset(arr, -1,sizeof(arr));
        cout << nCr(n, r) << "\n";
    }
}
