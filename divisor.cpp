#include<bits/stdc++.h>
using namespace std;

int main()
{
    vector <int> divisors[1000002];

    void divisors(int n)
    {
        int i, j;
        for(i = 1; i <= n; i++)
            for(j = i; j)
    }
}
